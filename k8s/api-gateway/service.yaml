apiVersion: v1
kind: Service

metadata:
  name: api-gateway
  namespace: currency-system
  labels:
    app: api-gateway

spec:
  selector:
    app: api-gateway

  ports:
    - port: 8765
      targetPort: 8765

  type: ClusterIP