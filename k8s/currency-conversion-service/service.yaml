apiVersion: v1
kind: Service

metadata:
  name: currency-conversion-service
  namespace: currency-system
  labels:
    app: currency-conversion-service

spec:
  selector:
    app: currency-conversion-service

  ports:
    - port: 8100
      targetPort: 8100

  type: ClusterIP