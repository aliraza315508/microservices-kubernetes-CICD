apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler

metadata:
  name: currency-conversion-service-hpa
  namespace: currency-system
  labels:
    app: currency-conversion-service

spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: currency-conversion-service

  minReplicas: 2
  maxReplicas: 5

  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70