apiVersion: apps/v1
kind: Deployment

metadata:
  name: currency-conversion-service
  namespace: currency-system
  labels:
    app: currency-conversion-service

spec:
  replicas: 2

  selector:
    matchLabels:
      app: currency-conversion-service

  template:
    metadata:
      labels:
        app: currency-conversion-service

    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault

      volumes:
        - name: tmp-volume
          emptyDir: {}

      containers:
        - name: currency-conversion-service
          image: currency-conversion-service:placeholder
          imagePullPolicy: Always

          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL

          volumeMounts:
            - name: tmp-volume
              mountPath: /tmp

          ports:
            - containerPort: 8100

          env:
            - name: SERVER_PORT
              value: "8100"

            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              valueFrom:
                configMapKeyRef:
                  name: common-config
                  key: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE

            - name: CONFIG_SERVER_URL
              valueFrom:
                configMapKeyRef:
                  name: common-config
                  key: CONFIG_SERVER_URL

          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "300m"
              memory: "512Mi"

          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8100
            failureThreshold: 30
            periodSeconds: 10
            timeoutSeconds: 5

          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8100
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 5

          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8100
            initialDelaySeconds: 90
            periodSeconds: 20
            timeoutSeconds: 5