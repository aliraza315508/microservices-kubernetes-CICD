apiVersion: v1
kind: Service

metadata:
  name: naming-server
  namespace: currency-system
  labels:
    app: naming-server

spec:
  selector:
    app: naming-server

  ports:
    - port: 8761
      targetPort: 8761

  type: ClusterIP